- name: Verificar conexión con los equipos
  hosts: localhost
  gather_facts: yes
  tasks:
    - name: Prueba de respuesta de equipos con ping
      ansible.builtin.ping:
      register: resultado_ping

    - name: Mostrar resultado del ping
      debug:
        var: resultado_ping

    - name: Mostrar version del SO
      ansible.builtin.debug:
        msg: "La version del software es: {{ ansible_facts['ansible_distribution'] }}"
